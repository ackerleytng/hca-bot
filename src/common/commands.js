import { setName, doSetName } from '../handlers/name.js';
import { setPatientId, doSetPatientId } from '../handlers/patientId.js';

const commands = {
  setName,
  doSetName,
  setPatientId,
  doSetPatientId,
};

export default commands;
